% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/box_plots.R
\name{boxplot_v}
\alias{boxplot_v}
\title{Makes vertical boxplots}
\usage{
boxplot_v(xcoord, y, n, inhgt = 20, whiskhgt = 5, bcol = "black",
  bfill = "grey", pcex = 1, lwd = 1, pch = 20)
}
\arguments{
\item{xcoord}{X coordinate for plotting boxplot}

\item{y}{Vector of the 6 boxplot statistics (e.g. 5, 25, 50, 75, 95th percentile and mean)}

\item{n}{Number of boxplots to be placed on plot}

\item{inhgt}{Height of the interquartile box, scaled to size of device and number of boxes to be plotted}

\item{whiskhgt}{Height of the 95th percentile whiskers, scaled in the same way as inhgt}

\item{bcol}{A 1 or 4 element vector of colors for whiskers, interquartile box, median line, and mean point}

\item{bfill}{Fill color for interquartile box}

\item{pcex}{Size of point indicating the mean}

\item{lwd}{A 1 or 3 element vector of widths for whiskers, interquartile box, and median line}
}
\value{
Boxplot
}
\description{
Makes vertical boxplots
}
\note{
Function transplanted from lmisc package
}
\examples{
x <- lapply(1:4, function(x) box_stats(sample(1:100, 10)))
plot(c(1, 100), c(0, 5), pch = "", xlab = "value", ylab = "", yaxt = "n")  
#for(i in 1:length(x)) boxplot_h(x[[i]], ycoord = i, n = 4)
#for(i in 1:length(x)) boxplot_h(x[[i]], ycoord = i, n = 4, pch = 5, pcex = 2)
#for(i in 1:length(x)) boxplot_h(x[[i]], ycoord = i, n = 4, bcol = "red", bfill = "grey", whiskcol = "grey")
for(i in 1:length(x)) {
  boxplot_h(x[[i]], ycoord = i, n = 4, bcol = c("black", "grey40", "red", "blue"), bfill = "grey80", 
            lwd = c(2, 1, 7), whiskcol = "grey")
}
}
